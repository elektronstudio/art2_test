{
  "name": "art2",
  "version": "0.0.1",
  "scripts": {
    "dev": "iles dev --open",
    "build": "iles build",
    "preview": "iles preview --open",
    "typecheck": "vue-tsc --noEmit"
  },
  "engines": {
    "node": "^14.0.0 || >= 16.0.0"
  },
  "devDependencies": {
    "iles": "^0.6.12",
    "vite": "^2.6.13",
    "vue-tsc": "^0.3.0"
  },
  "dependencies": {
    "cross-fetch": "^3.1.4"
  }
}
